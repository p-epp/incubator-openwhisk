[defaults]

callback_whitelist = profile_tasks
retry_files_enabled = False
host_key_checking = False
inventory = environments/aws
callback_plugins = callbacks
hash_behaviour = merge
remote_user = ubuntu
private_key_file=/home/ubuntu/openwhisk.pem

[ssh_connection]
scp_if_ssh = True
